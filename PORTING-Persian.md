# PORTING.md — چه چیزی، توسط چه کسی، و چگونه قابل استفاده مجدد است (خلاصه‌شده)

> **توجه**: این یک ترجمه‌ی خلاصه‌شده است. راهنمای فنی کامل با جزئیات کد و
> نکات دام‌گیر فقط در [PORTING.md](PORTING.md) اصلی موجود است — پیش از
> اقتباس واقعی یک الگو، با آن مشورت کنید.

خلاصه‌ای از الگوهای پیاده‌سازی قابل‌استفاده‌مجدد این پروژه، برای هر کسی
که می‌خواهد آن‌ها را به پروژه‌ای دیگر منتقل کند:

1. **`crates/directx-shader-translate`**: یک پارسر کانتینر/تکه‌ی DXBC
   (بایت‌کد شیدر D3D9/10/11، SM<=5.1) (`parse_dxbc`)، به‌علاوه یک مترجم
   DXBC(SM5.0)→SPIR-V با دامنه‌ی باریک (`translate_shader`) برای دقیقاً
   شکل‌های اپکدی که واقعاً توسط `fxc.exe` برای شیدرهای خود این پروژه
   ساطع می‌شوند: عملگرهای منفرد (جمع/ضرب/تفریق-از-طریق-جمع-منفی‌شده/
   تقسیم)، زنجیره‌های عملیات دودویی متوالی با طول دلخواه (مبتنی بر درخت
   عبارت رجیستر، نه ثابت‌کدسازی بر اساس طول — تا 7 جمله تأیید شده با
   *صفر* تغییر کد تولیدی برای هر تعداد جمله‌ی جدید)، و انواع دارای بررسی
   مرز (`if (id.x < N)`) از همین، که SPIR-V واقعی
   `OpSelectionMerge`/`OpBranchConditional` تولید می‌کند. هر شکل اپکد
   دیگری به‌جای ترجمه‌ی نادرست خاموش، از طریق
   `SpirvGenError::UnsupportedShader` به‌صراحت رد می‌شود — این **یک**
   رمزگشای عمومی SM5.0 **نیست**.
2. **پشتیبانی DXIL (SM6+، D3D12)** (`src/dxil.rs`): `parse_dxil_container`
   جریان بیت خام LLVM را (از طریق کریت عمومی `llvm-bitcode`) پیمایش
   می‌کند تا جدول نوع را حل و دستورالعمل‌های تابع را رمزگشایی کند؛
   خانواده‌ی توابع `resolve_vector_add_dxil_calls` فراخوانی‌های ذاتی
   `dx.op.*` در DXIL (CreateHandle/ThreadId/BufferLoad/BufferStore) را در
   برابر شماره‌های اپکد مستند DXIL.rst مایکروسافت تفکیک می‌کنند.
   `translate_dxil_..._to_spirv` یک ساطع‌کننده‌ی SPIR-V مشترک
   (`emit_spirv_for_kernel`) با بک‌اند DXBC به اشتراک می‌گذارد، پس هر دو
   فرمت کانتینر SPIR-V را از یک مسیر کد واحد تولید می‌کنند. اندازه‌ی گروه
   کاری (`numthreads`) واقعاً از `METADATA_BLOCK` مربوط به DXIL
   (`dx.entryPoints` → `ShaderProperties`) استخراج می‌شود، نه
   ثابت‌کدشده. تعمیم زنجیره‌ی دارای بررسی مرز (بالا) به‌طور سرتاسری روی
   هر دو DXBC و DXIL برای زنجیره‌های تا 7 جمله تأیید شده است.
3. **خط لوله‌ی گرافیکی D3D11**: `translate_vertex_shader`/
   `translate_pixel_shader` SPIR-V گرافیکی واقعی برای یک جفت VS/PS
   عبوری ثابت ساطع می‌کنند، به دو روش مستقل تأیید شده (بازتجزیه‌ی rspirv
   + `spirv-val.exe` واقعی از Vulkan SDK، هر دو کد خروج 0). کریت جدید
   `crates/directx-graphics-vulkan` وابستگی **مستقیم** خودِ این پروژه
   `ash` را اضافه می‌کند (نه لایه‌شده روی `opencuda-vulkan`، که با
   بازرسی سورس تأیید شد فقط compute-dispatch است) و رندر پس واقعی،
   فریم‌بافر، و `VkGraphicsPipelineCreateInfo` را پیاده‌سازی می‌کند. این
   کریت مثلث‌ها و صحنه‌های بافت‌دار/چند‌اسپرایتی را با ترکیب آلفای
   استاندارد «روی هم» و بارگذاری واقعی بافت از فایل PNG (کریت `png`)
   رسم و می‌خواند، همگی روی سخت‌افزار واقعی NVIDIA GT 730 (ویندوز) *و*
   WSL2 Ubuntu/Mesa llvmpipe (لینوکس)، با نتایج مطابق در هر دو
   سیستم‌عامل، تأیید شده.
4. **`crates/directx-graphics-window`**: یک پنجره‌ی واقعی + زنجیره‌ی
   تعویض واقعی Vulkan + ورودی صفحه‌کلید واقعی (winit + ash-window)، که
   نمونه‌ی مستقل خودش از Vulkan instance/device را جدا از زمینه‌ی
   خارج‌از‌صفحه‌ی `directx-graphics-vulkan` نگه می‌دارد — در صورت استفاده
   از هر دو، آن‌ها را همگام نگه دارید (مثلاً ترکیب آلفا فعلاً فقط در
   مسیر خارج‌از‌صفحه فعال است). یک نسخه‌نمایشی تعاملی سبک Breakout تولید
   شد، که با چشمان خودِ کاربر تأیید شد (حرکت پارو + جهش توپ).
5. **قرارداد وابستگی مبتنی بر مسیر**: این کریت به `opencuda-core`/
   `opencuda-vulkan` مربوط به `open-cuda` از طریق وابستگی‌های مسیر نسبی
   وابسته است، یک سطح دایرکتوری عمیق‌تر از قرارداد رایج «مخازن خواهر زیر
   `F:\runo`» که در سایر جاهای این اکوسیستم استفاده می‌شود (مثلاً
   `aruaru-llm`، `aruaru-db`) — این‌ها **فقط وابستگی‌های توسعه (dev-
   dependencies)** هستند، خودِ کتابخانه‌ی منتشرشده هیچ وابستگی‌ای به
   `open-cuda` ندارد.
6. **یادداشت دامنه‌ی ضدتقلب سطح کرنل**: برای هر کسی که این پروژه را به
   سمت هدف «اجرای بازی‌های واقعی ویندوز روی لینوکس» منتقل می‌کند —
   ضدتقلب حالت کرنل (Riot Vanguard، BattlEye حالت کرنل و غیره) محیط‌های
   سبک لینوکس/Proton را ذاتاً مسدود می‌کند، صرف‌نظر از کامل بودن این لایه‌ی
   ترجمه‌ی شیدر. این یک نقص برای رفع کردن نیست؛ عناوینی که از چنین
   ضدتقلبی استفاده می‌کنند، صرف‌نظر از کامل بودن ترجمه، خارج از دسترس
   این پروژه هستند.

## آنچه هنوز قابل استفاده مجدد نیست (شکاف‌های صادقانه)

- هیچ رمزگشای دستورالعمل عمومی SM5.0 یا SM6.0 وجود ندارد — فقط کلاس‌های
  خاص اپکد/شکل توصیف‌شده در بالا مدیریت می‌شوند؛ هر چیز دیگری به‌جای
  ترجمه‌ی نادرست، به‌صراحت رد می‌شود.
- لایه‌های بالاتر D3D12 (فهرست فرمان، انبوه‌های توصیفگر، امضاهای ریشه)
  کاملاً پیاده‌سازی‌نشده هستند.
- بدون بافر عمق، بدون بررسی میان‌یابی‌ رأس‌های رنگ‌متفاوت فراتر از حالت
  مثلث گرادیان، و سخت‌افزار GPU مربوط به AMD/Intel و اجرای بومی
  macOS/دسکتاپ لینوکس همچنان تأیید‌نشده باقی می‌مانند (مسیرهای کد برای
  تشخیص شناسه‌ی سازنده‌ی PCI مربوط به AMD/Intel وجود دارند اما هرگز روی
  سخت‌افزار واقعی AMD/Intel در این محیط توسعه اجرا نشده‌اند).

---

برای جزئیات فنی کامل (دنباله‌های دقیق اپکد، ردیابی سطح بایت جریان بیت
DXIL، قطعات کد، و نمونه‌های کامل Cargo.toml وابستگی مبتنی بر مسیر)، به
فایل اصلی [PORTING.md](PORTING.md) (انگلیسی، مرجع اصلی) مراجعه کنید.
