# PORTING.md — מה ניתן לשימוש חוזר, על ידי מי, ואיך (גרסה מקוצרת)

> **הערה**: זהו תרגום מקוצר. המדריך הטכני המלא עם פרטי קוד ומלכודות
> זמין רק ב-[PORTING.md](PORTING.md) המקורי — יש להתייעץ בו לפני אימוץ
> דפוס בפועל.

סיכום דפוסי מימוש הניתנים לשימוש חוזר מפרויקט זה, עבור כל מי שמעביר
אותם לפרויקט אחר:

1. **`crates/directx-shader-translate`**: מנתח מיכל/chunk של DXBC
   (bytecode שיידרים של D3D9/10/11, SM<=5.1) (`parse_dxbc`), בתוספת
   מתרגם DXBC(SM5.0)→SPIR-V בהיקף צר (`translate_shader`) עבור בדיוק
   צורות האופקוד שמופקות בפועל על ידי `fxc.exe` לשיידרים של פרויקט
   זה: פעולות יחידות (חיבור/כפל/חיסור-דרך-חיבור-שלילי/חילוק), שרשראות
   פעולות-בינאריות רצופות באורך שרירותי (מבוססות עץ ביטוי-רגיסטר, לא
   קידוד-קשיח לפי אורך — אומת עד 7 איברים עם **אפס** שינויי קוד
   production נדרשים לכל מספר איברים נוסף), ווריאנטים עם בדיקת
   גבולות (`if (id.x < N)`) של אותם דברים, המפיקים SPIR-V אמיתי מסוג
   `OpSelectionMerge`/`OpBranchConditional`. כל צורת אופקוד אחרת
   נדחית ביושר דרך `SpirvGenError::UnsupportedShader` ולא מתורגמת
   בשקט באופן שגוי — זהו **לא** מפענח SM5.0 כללי.
2. **תמיכת DXIL (SM6+, D3D12)** (`src/dxil.rs`): `parse_dxil_container`
   עובר על ה-LLVM bitstream הגולמי (דרך חבילת `llvm-bitcode` הכללית)
   כדי לפענח את טבלת הטיפוסים ולפענח הוראות פונקציה; פונקציות ממשפחת
   `resolve_vector_add_dxil_calls` מפרשות את קריאות ה-intrinsic
   `dx.op.*` של DXIL (CreateHandle/ThreadId/BufferLoad/BufferStore)
   כנגד מספרי האופקוד המתועדים ב-DXIL.rst של Microsoft.
   `translate_dxil_..._to_spirv` חולק emitter SPIR-V אחד
   (`emit_spirv_for_kernel`) עם ה-backend של DXBC, כך ששני פורמטי
   המיכל מפיקים SPIR-V דרך מסלול קוד יחיד. גודל קבוצת ה-threads
   (`numthreads`) מופק באמת מ-`METADATA_BLOCK` של DXIL
   (`dx.entryPoints` → `ShaderProperties`), לא קבוע בקוד. הכללת
   השרשרת עם בדיקת הגבולות (ראו למעלה) אומתה מקצה לקצה הן על DXBC
   והן על DXIL עבור שרשראות עד 7 איברים.
3. **צנרת גרפיקה D3D11**: `translate_vertex_shader`/
   `translate_pixel_shader` מפיקים SPIR-V גרפי אמיתי עבור זוג VS/PS
   קבוע של מעבר ישיר, אומת בשתי דרכים בלתי תלויות (פענוח מחדש של
   rspirv + `spirv-val.exe` האמיתי של Vulkan SDK, שניהם קוד יציאה 0).
   חבילה חדשה `crates/directx-graphics-vulkan` מוסיפה את `ash` כתלות
   **ישירה** משלה של פרויקט זה (לא בשכבה על גבי `opencuda-vulkan`,
   שאושר בביקורת קוד מקור כמשמש לריצת compute בלבד) ומממשת render
   pass, framebuffer, ו-`VkGraphicsPipelineCreateInfo` אמיתיים. היא
   מציירת וקוראת חזרה משולשים וסצנות טקסטורה/ספרייטים מרובים עם מיזוג
   אלפא סטנדרטי מסוג "over" וטעינת טקסטורות מקבצי PNG אמיתיים (חבילת
   `png`), כל זה אומת על חומרת NVIDIA GT 730 אמיתית (Windows) *וגם*
   WSL2 Ubuntu/Mesa llvmpipe (Linux), עם תוצאות תואמות בשתי מערכות
   ההפעלה.
4. **`crates/directx-graphics-window`**: חלון אמיתי + שרשרת-החלפה
   Vulkan אמיתית + קלט מקלדת אמיתי (winit + ash-window), המחזיקה
   instance/device Vulkan עצמאי משלה, נפרד מההקשר ה-offscreen של
   `directx-graphics-vulkan` — יש לשמור על סנכרון בין השניים אם
   משתמשים בשניהם (למשל מיזוג אלפא מופעל כרגע רק בנתיב ה-offscreen).
   הופקה הדגמת Breakout אינטראקטיבית, אושרה כעובדת בעיני המשתמש עצמו
   (תנועת מחבט + החזרת כדור).
5. **מוסכמת תלות-נתיב**: חבילה זו תלויה ב-`opencuda-core`/
   `opencuda-vulkan` של `open-cuda` דרך תלויות path יחסיות, רמה אחת
   עמוקה יותר מהמוסכמה של מאגרים-אחים תחת `F:\runo` הנהוגה במקומות
   אחרים באקוסיסטם (למשל `aruaru-llm`, `aruaru-db`) — אלה
   **dev-dependencies בלבד**, לחבילת הספרייה המפורסמת עצמה אין תלות
   ב-`open-cuda`.
6. **הערת היקף אנטי-רמאות ברמת kernel**: לכל מי שמעביר פרויקט זה
   לכיוון מטרה של "הרצת משחקי Windows אמיתיים על Linux" — אנטי-רמאות
   ברמת kernel (Riot Vanguard, BattlEye ברמת kernel וכו') חוסמת
   סביבות בסגנון Linux/Proton במעצם עיצובה, בלתי תלוי במידת השלמות
   שאליה מגיעה שכבת תרגום השיידרים הזו. זה לא פגם לתיקון; כותרים
   המשתמשים באנטי-רמאות כזו הם מחוץ להישג ידו של פרויקט זה ללא קשר
   לשלמות התרגום.

## מה **אינו** ניתן עדיין לשימוש חוזר (פערים כנים)

- אין מפענח הוראות SM5.0 או SM6.0 כללי — רק מחלקות אופקוד/צורה
  הספציפיות המתוארות למעלה מטופלות; כל דבר אחר נדחה ביושר, לא מתורגם
  באופן שגוי.
- השכבות ברמה גבוהה יותר של D3D12 (רשימות פקודות, ערמות descriptor,
  root signatures) אינן ממומשות כלל.
- אין depth buffer, אין בדיקת אינטרפולציה-בין-קודקודים-בצבעים-שונים
  מעבר למקרה משולש-הגרדיאנט, וחומרת GPU של AMD/Intel וריצה ילידית
  על macOS/Linux-desktop נשארות לא-מאומתות (נתיבי קוד קיימים לזיהוי
  PCI vendor-ID של AMD/Intel אך מעולם לא רצו מול חומרת AMD/Intel
  אמיתית בסביבת פיתוח זו).

---

לפירוט הטכני המלא (רצפי אופקוד מדויקים, עקבות bitstream של DXIL ברמת
byte, קטעי קוד, ודוגמאות Cargo.toml מלאות של תלויות path), ראו את
[PORTING.md](PORTING.md) המקורי (אנגלית, כתובה-במקור).
