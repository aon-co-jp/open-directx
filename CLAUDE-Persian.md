# فلسفه‌ی طراحی و خط‌مشی توسعه و قواعد محیط توسعه (open-directx) — خلاصه‌شده

> **توجه**: این ترجمه‌ی خلاصه‌شده‌ی وضعیت فعلی است. گزارش کامل تاریخی
> HANDOFF (ده‌ها ورودی از 2026-07-25 تاکنون) فقط به زبان ژاپنی در
> CLAUDE.md موجود است — برای جزئیات نشست‌به‌نشست به آنجا مراجعه کنید.

درایو کاری: `F:\runo`. این بخش از رویه‌ی تلقی
[`open-raid-z`](https://github.com/aon-co-jp/open-raid-z) به‌عنوان
`CLAUDE.md` منبع اصلی و کپی کردن آن در هر پروژه پیروی می‌کند. مخزن
گیت‌هاب: [aon-co-jp/open-directx](https://github.com/aon-co-jp/open-directx).

**تاریخ شروع توسعه: 2026-07-25** (خودِ مخزن خالی گیت‌هاب در 2026-07-01
ساخته شده بود).

## وظیفه‌ی معلق (افزوده‌شده در 2026-08-06، هنوز آغاز نشده)

طبق دستور کاربر، طرحی برای بررسی گنجاندن فناوری Simulated Bifurcation
Machine (شبه‌محاسبه‌ی کوانتومی) توشیبا و فناوری‌های DeepSeek (MLA،
DeepSeekMoE، آموزش دقت مختلط FP8، بررسی‌شده از مقالات/وبلاگ‌های
پیاده‌سازی، نه فقط اخبار) در 8 مخزن از جمله `open-directx` وجود دارد.
هنوز هیچ هدف بهینه‌سازی مشخصی برای این مخزن شناسایی نشده — بررسی به یک
نشست آینده موکول شده است.

## نقش این پروژه

یک لایه‌ی سازگاری DirectX (D3D9/10/11/12) چندسکویی که هدفش اجرای
برنامه‌ها/بازی‌های نوشته‌شده برای API مخصوص ویندوز DirectX روی لینوکس،
اندروید، و در نهایت macOS و خانواده‌ی PlayStation است.

در تاریخ 2026-07-25، به انتخاب صریح کاربر، این پروژه به دنبال یک **لایه‌ی
سازگاری واقعی در جهت معکوس** (اجرای باینری‌ها/شیدرهای بدون‌تغییر DirectX
ویندوز روی سیستم‌عامل‌های دیگر) متعهد شد، به‌جای گزینه‌ی جایگزین پیشین
«ارائه‌ی یک API شبیه DirectX روی پایه‌ی مشترک Vulkan».

## اصلاح جایگاه فنی (2026-07-25، مهم)

ارزیابی پیشین (2026-07-23) نتیجه گرفته بود که DXVK/vkd3d-proton/MoltenVK
فقط یک‌طرفه (DirectX → Vulkan/Metal) ترجمه می‌کنند بدون نمونه‌ی واقعی از
جهت معکوس، و بنابراین تناسب فنی ضعیفی دارند. این ارزیابی **آمیختن دو
محور مختلف** بود و اصلاح شده است:

- DXVK/vkd3d-proton (فناوری پشت Proton شرکت Valve / سازگاری بازی‌های
  DirectX نسخه‌ی لینوکس استیم) و CrossOver/Whisky مبتنی بر MoltenVK
  (macOS)، نمونه‌های واقعی و کارآمدی از دقیقاً همان سازگاری معکوسی هستند
  که کاربر می‌خواهد: اجرای باینری‌ها/بازی‌های واقعی موجود DirectX (یک API
  مخصوص ویندوز) بدون تغییر روی لینوکس/macOS.
- **جهت ترجمه** («فراخوانی‌های DirectX به فراخوانی‌های Vulkan ترجمه
  می‌شوند») و **جهت تجربه‌ی کاربر نهایی** («آیا یک برنامه‌ی هدف‌گرفته‌شده‌ی
  ویندوز DirectX واقعاً روی لینوکس/macOS اجرا می‌شود») محورهای جدایی
  هستند — Vulkan-محور بودن جهت اول، جهت دوم را از دستیابی صحیح به
  «اجرای DirectX روی سیستم‌عامل دیگر» بازنمی‌دارد.
- بنابراین، استفاده از Vulkan به‌عنوان بک‌اند اجرای داخلی با هدف پیگیری
  یک لایه‌ی سازگاری معکوس واقعی در تضاد نیست — DXVK و امثال آن دقیقاً
  همین سابقه هستند. این پروژه همان رویکرد را اتخاذ می‌کند: رهگیری
  فراخوانی‌های API D3D + ترجمه‌ی بایت‌کد شیدر DXBC/DXIL در زمان اجرا →
  اجرا از طریق Vulkan.

## دامنه و نقشه‌ی راه صادقانه

**فاز 0 (فعلی، مرحله‌ی طراحی/تحقیق)**:
- بررسی ساختار DXBC/DXIL (فرمت‌های بایت‌کد شیدر DirectX).
- مطالعه‌ی معماری پیاده‌سازی‌های موجود متن‌باز (DXVK، vkd3d-proton،
  [dxil-spirv](https://github.com/HansKristian-Work/dxil-spirv) — ابزار
  واقعی DXIL→SPIR-V که vkd3d-proton از آن استفاده می‌کند، SPIRV-Cross،
  naga) برای اجتناب از بازاختراع چرخ و اقتباس تصمیمات طراحی اثبات‌شده.
- تعیین دامنه‌ی واقع‌بینانه‌ی MVP: **یک خط لوله‌ی گرافیکی کامل
  (رسترایزر، نمونه‌بردار بافت، حالت ترکیب و غیره) فعلاً خارج از دامنه
  است.** شروع با یک برش عمودی که فقط dispatch شیدر D3D11 Compute
  (DirectCompute) را پوشش می‌دهد — یک شیدر compute ساده که واقعاً از
  DXBC/DXIL به SPIR-V ترجمه شده، از طریق `opencuda-vulkan` مربوط به
  `open-cuda` ارسال شده، و تأیید شده که با یک پیاده‌سازی مرجع CPU تطابق
  عددی دارد. کار خط لوله‌ی گرافیکی به‌عنوان فاز بعدی فقط پس از اثبات این
  برش عمودی آغاز می‌شود.

**فاز 1 به بعد (هنوز آغاز نشده)**:
1. برش عمودی شیدر D3D11 Compute (ترجمه‌ی DXBC/DXIL→SPIR-V + ارسال
   Vulkan).
2. خط لوله‌ی گرافیکی حداقلی D3D11 (شیدرهای ورتکس/پیکسل + رسترایزیشن
   پایه).
3. پشتیبانی D3D12 (فهرست فرمان، انبوه‌های توصیفگر، امضاهای ریشه).
4. پشتیبانی اندروید (خودِ Vulkan بومی اندروید است، پس بیشتر دارایی‌های
   نسخه‌ی لینوکس باید قابل استفاده مجدد باشند — اما احتمالاً به یک لایه‌ی
   شبیه‌سازی Win32/COM، معادل Wine، نیاز خواهد بود؛ در آن صورت همکاری/
   استفاده‌ی مجدد از خودِ پروژه‌ی Wine را در نظر بگیرید).
5. پشتیبانی macOS / iPhone / iPad (از طریق MoltenVK، همان رویکرد
   CrossOver/Whisky؛ iPhone/iPad برای توزیع رسمی نیازمند Apple Developer
   Program است — اجرای بومی روی سخت‌افزار غیررسمی غیرممکن است، همان
   محدودیت شناسایی‌شده در تحقیقات `dream-os`).
6. سیستم‌های مختلف خانواده‌ی یونیکس (BSD و غیره) — احتمالاً قادر به
   استفاده‌ی مجدد از بیشتر مسیر لینوکس بسته به پشتیبانی Vulkan (هنوز
   بررسی نشده).

**درباره‌ی پشتیبانی PlayStation 4/5/6/7 (افشای صادقانه، از 2026-07-25)**:
در چشم‌انداز اصلی کاربر گنجانده شده، اما **نگرانی‌های حقوقی/شرایط خدمات
مستقل از دشواری فنی وجود دارد** — SDKهای توسعه‌ی PlayStation خصوصی و
تحت NDA هستند، و مهندسی معکوس غیررسمی خطر نقض شرایط خدمات مختلف و قوانین
(مانند DMCA) را دارد. این پروژه پشتیبانی PS4-7 را فقط به‌عنوان یک
**«آرزوی آینده»** در نقشه‌ی راه ثبت می‌کند و فعلاً آن را در دامنه‌ی
طراحی/پیاده‌سازی نمی‌گنجاند. شروع آن نیازمند ارزیابی جداگانه‌ی ریسک
حقوقی و تأیید مجدد کاربر خواهد بود.

**درباره‌ی پشتیبانی Nintendo Switch 2/3 (افزوده‌شده در 2026-08-17، افشای
صادقانه)**: به‌طور مشابه فقط به‌عنوان «آرزوی آینده» ثبت شده است. Switch 2
نیازمند سخت‌افزار توسعه/NDA رسمی نینتندو است (همان نگرانی حقوقی مانند
PS4-7). **Switch 3 تا تاریخ 2026-08-17 به‌طور رسمی از سوی نینتندو اعلام
نشده — این گنجاندن فقط بر اساس اطلاعات واقعی محصول نیست بلکه صرفاً
جانگهدار برای «اگر و وقتی اعلام/عرضه شد» است** (به‌صراحت برای جلوگیری از
اغراق ذکر شده).

## پروژه‌های پایه (طبق دستور کاربر، 2026-07-25)

- **[open-cuda](https://github.com/aon-co-jp/open-cuda)**: از
  `opencuda-vulkan` (بک‌اند اجرای Vulkan Compute، روی سخت‌افزار واقعی
  NVIDIA GT 730 تأیید شده) به‌عنوان بک‌اند اجرای شیدر استفاده می‌کند.
  برنامه‌ریزی برای استفاده‌ی مجدد از انتزاع `opencuda-core::GpuDevice`
  (alloc/memcpy/launch_kernel) به‌همان شکل، با عبور دادن هسته‌های
  ترجمه‌شده‌ی DXBC/DXIL→SPIR-V به‌عنوان `KernelSource::SpirV` (جزئیات
  دقیق API باید در برابر `opencuda-core` تأیید شود). متمایز از
  `opencuda-directx` (یک بک‌اند D3D12 مخصوص ویندوز، فاز 1 و 2 از پیش
  پیاده‌سازی‌شده) — آن یک بک‌اند «اجرای مستقیم DirectX روی ویندوز» است،
  جهت مخالف این پروژه (اجرای DirectX روی سیستم‌عامل‌های دیگر).
- **[aruaru-llm](https://github.com/aon-co-jp/aruaru-llm)**: در حال
  حاضر بدون وابستگی فنی مستقیم (aruaru-llm یک سرویس استنتاج LLM است؛
  این پروژه یک لایه‌ی سازگاری API گرافیکی است). نیت دقیق کاربر پشت
  فهرست کردن aruaru-llm به‌عنوان «پایه» تأیید نشده — احتمالاً به معنای
  پیروی از الگوی سرویس‌سازی مشترک آن (اعمال الگوی API مدیریتی سبک
  `TenantRegistry` آن به یک سطح مدیریتی این پروژه، مانند سروری برای
  حافظه‌ی پنهان ترجمه). پس از شناسایی نقاط یکپارچگی مشخص، این بخش
  به‌روزرسانی خواهد شد.

## خط‌مشی توسعه (خلاصه‌ی سراسر اکوسیستم)

- پیاده‌سازی مبتنی بر Rust. از کریت `windows` (API ویندوز) و بایندینگ‌های
  Vulkan (`ash` و غیره، مطابق آنچه `opencuda-vulkan` استفاده می‌کند)
  استفاده می‌شود.
- «هرگز از موفقیت type-check/build به‌تنهایی به‌عنوان تکمیل‌شده گزارش
  ندهید» — فقط پس از ترجمه‌ی واقعی بایت‌کد واقعی DXBC/DXIL، ارسال
  Vulkan واقعی، و تأیید تطابق عددی در برابر یک پیاده‌سازی مرجع CPU روی
  سخت‌افزار واقعی، «کار کرد» را گزارش دهید (این یک انضباط سراسر اکوسیستم
  است).
- ویژگی‌های پیاده‌سازی‌نشده/استاب هرگز نباید سیگنال نادرست «پشتیبانی
  می‌شود» بدهند (پیروی از الگوی `supports_dxil()` در `opencuda-directx`).
- پیش از هر تصمیم مربوط به فایل‌های جدید، کریت‌های جدید، مخازن جدید، یا
  قضاوت‌های نام‌گذاری/جانمایی، با کاربر مشورت کنید (درس آموخته از
  2026-07-23، به CLAUDE.md مربوط به `open-raid-z` مراجعه کنید).

## HANDOFF (فقط ورودی‌های اخیر — برای گزارش کامل به CLAUDE.md مراجعه کنید)

- **2026-08-20: یک برش عمودی compute shader برای GEMM (اندازه ثابت
  2x2) پیاده‌سازی و روی سخت‌افزار واقعی تأیید شد**، به‌عنوان اولین گام
  به‌سوی شتاب‌دهی سخت‌افزاری برای استنتاج LLM. تابع
  `translate_gemm2x2_shader` به `directx-shader-translate` اضافه شد:
  یک شیدر HLSL برای GEMM 2x2x2 واقعاً با `fxc.exe` کامپایل شد، DXBC آن
  رمزگشایی و به SPIR-V ترجمه شد، سپس با استفاده از قرارداد کرنل
  `opencuda-vulkan` روی Vulkan واقعی (NVIDIA GT 730) اجرا شد. خروجی
  GPU دقیقاً با نتیجهٔ مرجع ضرب ماتریسی روی CPU مطابقت داشت. محدودیت
  صادقانه: فقط اندازهٔ ثابت 2x2 — GEMM عمومی با اندازهٔ متغیر (شامل
  حلقه)، عملیات Attention و اتصال به `aruaru-llm` خارج از محدودهٔ این
  مرحله باقی می‌مانند.
- **2026-08-19: بررسی امکان افزودن مکانیزم به‌روزرسانی خودکار (مشابه
  self-update مبتنی بر GitHub Releases در `open-english`) — به تعویق
  افتاد.** تنها باینری دارای `fn main` در این workspace
  `directx-graphics-window` است (دموی سبک Breakout برای بازرسی
  چشمی) که نه سرویس دائمی دارد و نه نقطهٔ پایانی `/healthz`، بنابراین
  این الگو قابل‌اعمال نیست.
- **2026-08-19: بررسی این ادعا که «نبود سرویس پس‌زمینهٔ دائمی یک نقص
  است» — نتیجه: خیر.** DirectX واقعی مایکروسافت به‌صورت DLLهای زمان
  اجرا عرضه می‌شود که برنامه‌ها به‌طور پویا به آن‌ها لینک می‌شوند، نه
  به‌عنوان یک سرویس پس‌زمینهٔ مستقل؛ طراحی فعلی (فقط کتابخانه + باینری
  دمو) با معماری DirectX اصلی مطابقت دارد.
- **2026-08-20: یادداشت ایدهٔ `open-cg-cad` ثبت شد (هنوز آغاز نشده).**
  مفهوم یک ابزار مدل‌سازی سه‌بعدی آینده با موشن‌کپچر، تغییر مشخصات به
  زبان طبیعی از طریق چت هوش مصنوعی، و پشتیبانی از
  DirectX/OpenGL/WebGL/WebGPU.
- **2026-08-20 (ادامه): مفهوم «املاک هوشمند × ساخت‌وساز هوشمند» ثبت
  شد**، که `open-cg-cad` را با `aruaru-llm` ترکیب می‌کند تا از داده‌های
  زمین به‌طور خودکار مدل‌های سه‌بعدی (خانه، ساختمان، پل، تونل و غیره)
  تولید کند؛ بعداً به قطارهای مغناطیسی (ماگلو) و مدل‌های CAD نیمه‌هادی
  (CPU/GPU/NPU) نیز گسترش یافت، همراه با یادآوری صادقانه دربارهٔ دشواری
  پوشش معماری، مهندسی عمران، ناوگان ریلی و طراحی نیمه‌هادی در یک سیستم
  واحد. فقط یادداشت ایده، هنوز آغاز نشده.

- **2026-08-08 (ادامه 12): بارگذاری واقعی بافت از فایل PNG پیاده‌سازی
  شد، روی سخت‌افزار واقعی ویندوز و لینوکس تأیید شد.** ماژول جدید
  `png_loader.rs` (با استفاده از کریت `png`، سری 0.17.x) تابع
  `load_png_rgba8` را پیاده‌سازی می‌کند و تصاویر RGB/RGBA/سطح‌خاکستری/
  سطح‌خاکستری+آلفا/پالت را به RGBA8 نرمال‌سازی می‌کند (بسط پالت و
  نرمال‌سازی 16بیت→8بیت هر دو به تبدیل‌های خودِ کریت `png` سپرده شده،
  دست‌ساز پیاده‌سازی نشده). یک دارایی واقعی تست
  (`assets/sample_sprite.png`، یک شطرنجی 2×2 با یک ربع نیمه‌شفاف) تولید
  و کامیت شد. روی سخت‌افزار واقعی ویندوز (NVIDIA GT 730) و لینوکس واقعی
  (WSL2 Ubuntu/Mesa llvmpipe) تأیید شد: ربع‌های تیره کاملاً مطابقت
  دارند، و ربع نیمه‌شفاف رنگ ترکیبی دقیقی که فرمول استاندارد «روی هم»
  پیش‌بینی می‌کند تولید می‌کند. کل فضای کاری: `cargo build`/`clippy`
  تمیز (0 هشدار)؛ `cargo test --workspace --release` همه‌ی 33 تست
  سخت‌افزار واقعی + 56 تست واحد را بدون رگرسیون رد می‌کند. افشای
  صادقانه: PNGهای تودرهم و PNGهای 16بیت‌بر‌کانال واقعاً تست نشده‌اند
  (فقط از طریق مدیریت خودکار کریت `png` تکیه شده)؛
  `directx-graphics-window` (نسخه‌نمایشی پنجره‌ی واقعی) هنوز این
  بارگذار را فراخوانی نمی‌کند (همچنان بافت تک‌رنگ 1×1 است).

- **2026-08-08 (ادامه 11): ترکیب آلفا (اسپرایت‌های نیمه‌شفاف) پیاده‌سازی
  شد، روی سخت‌افزار واقعی ویندوز و لینوکس تأیید شد.** ترکیب آلفای
  استاندارد «روی هم» در ساخت خط لوله‌ی `render_sprites_and_read_back`
  فعال شد (`SRC_ALPHA`/`ONE_MINUS_SRC_ALPHA`/`ADD`). چون
  `src.a=1.0` عددی معادل `blend_enable=false` است، همه‌ی تست‌های موجود
  اسپرایت تیره تأیید شدند که نتایج یکسانی تولید می‌کنند (تغییر
  افزایشی و بدون شکست). تست جدید نتیجه‌ی فرمول استاندارد «روی هم» را روی
  سخت‌افزار واقعی، هر دو سیستم‌عامل با تطابق دقیق، تأیید کرد. افشای
  صادقانه: این ترکیب فقط در مسیر خارج‌از‌صفحه‌ی
  `render_sprites_and_read_back` فعال است — `directx-graphics-window`
  (نسخه‌نمایشی پنجره‌ی واقعی) هنوز به‌روزرسانی نشده تا مطابقت داشته
  باشد؛ فقط ترکیب «روی هم» پشتیبانی می‌شود (بدون حالت‌های افزایشی/
  ضربی)؛ تعامل بررسی عمق/ترتیب ترکیب تست نشده (این گذر بدون بافر عمق،
  فقط اسپرایت دوبعدی است).

برای گزارش کامل نشست‌به‌نشست (شامل نقطه‌ی عطف پیشین خط لوله‌ی گرافیکی
D3D11، برش عمودی DXIL، حلقه‌ی بازی با پنجره‌ی واقعی + زنجیره‌ی تعویض +
ورودی صفحه‌کلید، و افزایش‌های متعدد طول زنجیره‌ی DXBC/DXIL دارای بررسی
مرز)، به [CLAUDE.md](CLAUDE.md) (ژاپنی، مرجع اصلی) مراجعه کنید.
