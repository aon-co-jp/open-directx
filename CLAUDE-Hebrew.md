# פילוסופיית עיצוב ומדיניות פיתוח וכללי סביבת פיתוח (open-directx) — גרסה מקוצרת

> **הערה**: זהו תרגום מקוצר של המצב הנוכחי. יומן ה-HANDOFF ההיסטורי
> המלא (עשרות רשומות מאז 2026-07-25) זמין רק ביפנית ב-CLAUDE.md — ראו
> שם לפירוט לפי סשן.

כונן העבודה: `F:\runo`. סעיף זה פועל לפי הנוהג שבו
[`open-raid-z`](https://github.com/aon-co-jp/open-raid-z) מהווה את ה-
`CLAUDE.md` הכתוב-במקור (source of truth), אשר מועתק לכל פרויקט.
מאגר GitHub: [aon-co-jp/open-directx](https://github.com/aon-co-jp/open-directx).

**תאריך תחילת פיתוח: 2026-07-25** (מאגר ה-GitHub הריק עצמו נוצר
ב-2026-07-01).

## משימה ממתינה (נוספה ב-2026-08-06, טרם החלה)

קיימת תוכנית, לפי הנחיית המשתמש, לחקור שילוב טכנולוגיית ה-Simulated
Bifurcation Machine (מחשוב פסאודו-קוונטי) של טושיבה וטכניקות DeepSeek
(MLA, DeepSeekMoE, אימון מדויק-מעורב FP8, שנחקרו ממאמרים/בלוגים של
ידע מימוש, לא רק מחדשות) לתוך 8 מאגרים כולל `open-directx`. עדיין לא
זוהתה מטרת אופטימיזציה קונקרטית עבור מאגר זה — החקירה נדחית לסשן
עתידי.

## תפקיד הפרויקט

שכבת תאימות DirectX (D3D9/10/11/12) חוצת-פלטפורמות ששואפת להריץ
יישומים/משחקים קיימים שנכתבו עבור ה-API בלעדי-Windows DirectX על
Linux, Android, ובעתיד גם macOS ומשפחת PlayStation.

ב-2026-07-25, לפי בחירה מפורשת של המשתמש, הפרויקט התחייב לרדוף אחר
**שכבת תאימות הפוכה-אמת** (הרצת בינאריים/שיידרים של Windows DirectX
ללא שינוי על מערכות הפעלה אחרות) במקום החלופה הקודמת של "חשיפת API
דמוי-DirectX מעל Vulkan כבסיס משותף".

## תיקון מיקום טכני (2026-07-25, חשוב)

הערכה קודמת (2026-07-23) קבעה ש-DXVK/vkd3d-proton/MoltenVK מתרגמים רק
בכיוון אחד (DirectX → Vulkan/Metal) ללא דוגמה אמיתית לכיוון ההפוך,
ולכן הותאמו טכנית בצורה גרועה — קביעה זו הייתה **בלבול בין שני צירים
שונים** ותוקנה:

- DXVK/vkd3d-proton (הטכנולוגיה שמאחורי Proton של Valve / תאימות
  משחקי DirectX ב-Linux Steam) ו-CrossOver/Whisky מבוססי-MoltenVK
  (macOS), הן דוגמאות אמיתיות ועובדות בדיוק לתאימות ההפוכה שהמשתמש
  מבקש: הרצת בינאריים/משחקים אמיתיים וקיימים של DirectX (API
  בלעדי-Windows) ללא שינוי על Linux/macOS.
- **כיוון התרגום** ("קריאות DirectX מתורגמות לקריאות Vulkan")
  ו-**כיוון חוויית משתמש הקצה** ("האם יישום DirectX המיועד ל-Windows
  אכן רץ על Linux/macOS") הם צירים נפרדים — היותו של הראשון מכוון
  ל-Vulkan אינו מונע מהאחרון להשיג נכון "הרצת DirectX על מערכת הפעלה
  אחרת".
- לפיכך, שימוש ב-Vulkan כ-backend ביצוע פנימי אינו סותר רדיפה אחר שכבת
  תאימות הפוכה-אמת — DXVK וכו' הם בדיוק התקדים הזה. פרויקט זה מאמץ
  אותה גישה (יירוט קריאות API של D3D + תרגום זמן-ריצה של bytecode
  שיידרים DXBC/DXIL → ביצוע דרך Vulkan).

## היקף ומפת דרכים כנה

**שלב 0 (נוכחי, שלב עיצוב/מחקר)**:
- מחקר מבנה DXBC/DXIL (פורמטי bytecode של שיידרי DirectX).
- לימוד ארכיטקטורת מימושי OSS קיימים (DXVK, vkd3d-proton,
  [dxil-spirv](https://github.com/HansKristian-Work/dxil-spirv) — כלי
  ה-DXIL→SPIR-V האמיתי ש-vkd3d-proton משתמש בו, SPIRV-Cross, naga)
  כדי להימנע מהמצאה מחדש של הגלגל ולשאול החלטות עיצוב מוכחות.
- חיתוך היקף MVP ריאלי: **צנרת גרפיקה מלאה (ראסטריזטור, דוגם
  טקסטורות, מצב blend וכו') מחוץ להיקף כרגע.** להתחיל בפרוסה אנכית
  המכסה רק שליחת D3D11 Compute Shader (DirectCompute) — שיידר compute
  פשוט אחד המתורגם בפועל מ-DXBC/DXIL ל-SPIR-V, נשלח דרך
  `opencuda-vulkan` של `open-cuda`, ומאומת שהוא תואם מספרית למימוש
  ייחוס CPU. עבודת צנרת הגרפיקה מתחילה כשלב הבא רק לאחר שהפרוסה
  האנכית הזו הוכחה.

**שלב 1 ואילך (טרם החל)**:
1. פרוסה אנכית של D3D11 Compute Shader (תרגום DXBC/DXIL→SPIR-V +
   שליחת Vulkan).
2. צנרת גרפיקה מינימלית של D3D11 (vertex/pixel shaders +
   ראסטריזציה בסיסית).
3. תמיכת D3D12 (רשימות פקודות, ערמות descriptor, root signatures).
4. תמיכת Android (Vulkan עצמה ילידית ל-Android, כך שרוב הנכסים של
   גרסת Linux אמורים להיות ניתנים לשימוש חוזר — אך שכבת אמולציית
   Win32/COM, מקבילה ל-Wine, תידרש כנראה, ובמקרה כזה יישקל שיתוף
   פעולה/שימוש חוזר עם פרויקט Wine עצמו).
5. תמיכת macOS / iPhone / iPad (דרך MoltenVK, אותה גישה כמו
   CrossOver/Whisky; iPhone/iPad דורש את Apple Developer Program
   להפצה רשמית — ריצה ילידית על חומרה לא-רשמית בלתי אפשרית, אותה
   מגבלה שזוהתה במחקר של `dream-os`).
6. מערכות שונות ממשפחת UNIX (BSD וכו') — סביר שניתן יהיה לעשות שימוש
   חוזר ברוב נתיב ה-Linux בהתאם לתמיכת Vulkan (טרם נחקר).

**בנוגע לתמיכת PlayStation 4/5/6/7 (גילוי כן, נכון ל-2026-07-25)**:
נכלל בחזון המקורי של המשתמש, אך **קיימות חששות משפטיים/תנאי-שירות
בנפרד מהקושי הטכני** — SDK-י הפיתוח של PlayStation פרטיים ותחת NDA,
והנדסה לאחור לא-רשמית מסתכנת בהפרה של תנאי שירות שונים וחוקים (למשל
DMCA). פרויקט זה שומר על תמיכת PS4-7 מצוינת במפת הדרכים רק כ-
**"שאיפה עתידית"** ואינה כלולה כרגע בהיקף העיצוב/המימוש. התחלת עבודה
בכיוון זה תדרוש הערכת סיכון משפטית נפרדת ואישור מחודש מהמשתמש.

**בנוגע לתמיכת Nintendo Switch 2/3 (נוסף ב-2026-08-17, גילוי כן)**:
באופן דומה, נרשם רק כ-"שאיפה עתידית". Switch 2 דורש ציוד/NDA פיתוח
רשמי של נינטנדו (אותו חשש משפטי כמו PS4-7). **Switch 3 טרם הוכרז
רשמית על ידי נינטנדו נכון ל-2026-08-17 — הכללה זו היא רק מציין מקום
למקרה של הכרזה/יציאה, לא מבוססת על מידע מוצר אמיתי** (צוין במפורש
כדי לא להגזים).

## פרויקטי בסיס (הנחיית משתמש, 2026-07-25)

- **[open-cuda](https://github.com/aon-co-jp/open-cuda)**: משתמש
  ב-`opencuda-vulkan` (backend ביצוע Vulkan Compute, אומת על חומרת
  NVIDIA GT 730 אמיתית) כ-backend ביצוע שיידרים. תוכנית לעשות שימוש
  חוזר ב-abstraction `opencuda-core::GpuDevice` (alloc/memcpy/
  launch_kernel) כפי שהוא, ולהעביר kernels שתורגמו DXBC/DXIL→SPIR-V
  כ-`KernelSource::SpirV` (פרטי API מדויקים לאישור מול
  `opencuda-core`). נבדל מ-`opencuda-directx` (backend D3D12 בלעדי
  ל-Windows, שלבים 1&2 כבר מומשו) — זה מריץ DirectX באופן ילידי *על*
  Windows, הכיוון ההפוך מפרויקט זה (הרצת DirectX *על מערכות הפעלה
  אחרות*).
- **[aruaru-llm](https://github.com/aon-co-jp/aruaru-llm)**: אין תלות
  טכנית ישירה כרגע (aruaru-llm הוא שירות היסק LLM; פרויקט זה הוא שכבת
  תאימות API גרפי). הכוונה המדויקת מאחורי רישום aruaru-llm כ"בסיס"
  בהנחיית המשתמש אינה מאושרת — ייתכן שהכוונה היא לעקוב אחר דפוס
  השירות המשותף "בונשין-נו-ג'וטסו" (שיבוט/דייר) שלו (למשל יישום דפוס
  ה-API הניהולי בסגנון `TenantRegistry` שלו על משטח ניהולי כלשהו של
  פרויקט זה, כגון שרת מטמון תרגום). יעודכן ברגע שיזוהו נקודות
  אינטגרציה קונקרטיות.

## מדיניות פיתוח (סיכום חוצה-אקוסיסטם)

- מימוש מבוסס Rust. שימוש בחבילת `windows` (Windows API) ו-bindings
  ל-Vulkan (`ash` וכו', בהתאמה למה ש-`opencuda-vulkan` משתמש בו).
- "לעולם אל תדווח על סיום רק מבדיקת טיפוסים/בנייה מוצלחת" — לדווח
  על משהו כ"עובד" רק לאחר תרגום בפועל של bytecode אמיתי של DXBC/DXIL,
  שליחה דרך Vulkan אמיתי, ואישור התאמה מספרית למימוש ייחוס CPU על
  חומרה אמיתית (משמעת חוצת-אקוסיסטם).
- תכונות שלא מומשו/stub לעולם לא ישדרו סיגנל שקרי של "נתמך" (בעקבות
  דפוס ה-`supports_dxil()` של `opencuda-directx`).
- אישור מול המשתמש לפני כל החלטה הכוללת קבצים חדשים, חבילות (crates)
  חדשות, מאגרים חדשים, או שיפוט קריטריוני מיקום/שם (לקח מ-2026-07-23,
  ראו CLAUDE.md של `open-raid-z`).

## HANDOFF (רשומות אחרונות בלבד — ראו CLAUDE.md ליומן המלא)

- **2026-08-08 (המשך 12): טעינת טקסטורות מקבצי PNG אמיתיים מומשה,
  אומתה על חומרת Windows + Linux אמיתית.** מודול חדש `png_loader.rs`
  (משתמש בחבילת `png`, גרסה 0.17.x) מממש `load_png_rgba8`, המנרמל
  תמונות RGB/RGBA/גווני-אפור/גווני-אפור+אלפא/פלטה ל-RGBA8 (הרחבת
  פלטה ונרמול 16-ביט→8-ביט מואצלים לטרנספורמציות של חבילת `png`
  עצמה, לא מיושמים ידנית). נכס בדיקה אמיתי
  (`assets/sample_sprite.png`, לוח שחמט 2x2 עם רבע אחד חצי-שקוף) נוצר
  והוכנס למאגר. אומת הן על Windows אמיתי (NVIDIA GT 730) והן על
  Linux אמיתי (WSL2 Ubuntu/Mesa llvmpipe): הרבעים האטומים תואמים
  בדיוק, והרבע החצי-שקוף מפיק בדיוק את צבע ההרכבה הצפוי לפי נוסחת
  "over" הסטנדרטית. מרחב עבודה מלא: `cargo build`/`clippy` נקי (0
  אזהרות); `cargo test --workspace --release` עובר את כל 33 בדיקות
  החומרה האמיתית + 56 בדיקות יחידה, ללא רגרסיות. גילוי כן: תמונות
  PNG משוזרות (interlaced) ותמונות 16-ביט-לערוץ לא נבדקו בפועל (הסתמכות
  על טיפול אוטומטי של חבילת `png` בלבד); `directx-graphics-window`
  (הדגמת חלון אמיתי) עדיין לא קוראת למטען זה (עדיין טקסטורת צבע-אחיד
  1x1).

- **2026-08-08 (המשך 11): מיזוג אלפא (ספרייטים חצי-שקופים) מומש, אומת
  על חומרת Windows + Linux אמיתית.** מיזוג אלפא סטנדרטי מסוג "over"
  הופעל בבניית הצנרת של `render_sprites_and_read_back`
  (`SRC_ALPHA`/`ONE_MINUS_SRC_ALPHA`/`ADD`). מכיוון ש-`src.a=1.0`
  שקול מספרית ל-`blend_enable=false`, כל בדיקות הספרייטים האטומים
  הקיימות אושרו כמפיקות תוצאות זהות (שינוי לא-שובר, תוסף). בדיקה
  חדשה אימתה את תוצאת הנוסחה הסטנדרטית "over" על חומרה אמיתית, שתי
  מערכות ההפעלה תואמות בדיוק. גילוי כן: מיזוג זה מופעל רק בנתיב
  ה-offscreen `render_sprites_and_read_back` — `directx-graphics-window`
  (הדגמת החלון האמיתי) לא עודכנה להתאים עדיין; נתמך רק מיזוג "over"
  (ללא מצבי תוספת/כפל); אינטראקציית depth-test/סדר-blend לא נבדקה
  (בשלב זה אין depth buffer, ספרייטים דו-ממדיים בלבד).

לתולדת הסשנים המלאה (כולל אבן הדרך המוקדמת יותר של צנרת גרפיקה
D3D11, הפרוסה האנכית של DXIL, לולאת המשחק חלון-אמיתי + שרשרת-החלפה +
קלט-מקלדת, ותוספות אורך-שרשרת DXBC/DXIL עם בדיקת-גבולות הרבות), ראו
[CLAUDE.md](CLAUDE.md) (יפנית, כתובה-במקור).
