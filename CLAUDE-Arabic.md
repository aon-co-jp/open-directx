# فلسفة التصميم وسياسة التطوير وقواعد بيئة التطوير (open-directx) — نسخة مختصرة

> **ملاحظة**: هذه ترجمة مختصرة للحالة الحالية. يبقى سجل HANDOFF
> التاريخي الكامل (عشرات المدخلات منذ 2026-07-25) متاحًا فقط باللغة
> اليابانية في ملف CLAUDE.md — انظر هناك للتفاصيل جلسة بجلسة.

قرص العمل: `F:\runo`. يتبع هذا القسم ممارسة اعتبار ملف `CLAUDE.md`
الخاص بمشروع [`open-raid-z`](https://github.com/aon-co-jp/open-raid-z)
مصدر الحقيقة الأساسي ونسخه إلى كل مشروع. مستودع GitHub:
[aon-co-jp/open-directx](https://github.com/aon-co-jp/open-directx).

**تاريخ بدء التطوير: 2026-07-25** (تم إنشاء مستودع GitHub نفسه فارغًا
بتاريخ 2026-07-01).

## مهمة معلّقة (أُضيفت 2026-08-06، لم تبدأ بعد)

هناك خطة، بتعليمات من المستخدم، لبحث دمج تقنية Toshiba's Simulated
Bifurcation Machine (حوسبة شبه كمومية) وتقنيات DeepSeek (MLA،
DeepSeekMoE، تدريب دقة مختلطة FP8، مبحوثة من أوراق علمية/مدونات تنفيذ،
وليس فقط الأخبار) في 8 مستودعات منها `open-directx`. لم يُحدَّد بعد
أي هدف تحسين ملموس لهذا المستودع — البحث مؤجَّل لجلسة مستقبلية.

## دور هذا المشروع

طبقة توافقية لـ DirectX (D3D9/10/11/12) عبر الأنظمة، تهدف إلى تشغيل
تطبيقات/ألعاب موجودة مكتوبة باستخدام API الخاص بـ DirectX (الحصري
لـ Windows) على Linux وAndroid ومستقبلًا macOS وعائلة PlayStation.

في 2026-07-25، وباختيار صريح من المستخدم، التزم المشروع بمتابعة
**طبقة توافقية حقيقية بالاتجاه المعاكس** (تشغيل ملفات ثنائية/تظليلات
DirectX الخاصة بـ Windows دون تعديل على أنظمة تشغيل أخرى) بدلًا من
البديل السابق "توفير API شبيه بـ DirectX فوق Vulkan كقاعدة مشتركة".

## تصحيح الموقف التقني (2026-07-25، مهم)

كان تقييم سابق (2026-07-23) قد حكم بأن DXVK/vkd3d-proton/MoltenVK
تترجم فقط باتجاه واحد (DirectX → Vulkan/Metal) بدون مثال حقيقي
للاتجاه المعاكس، وبالتالي فهي غير مناسبة تقنيًا. كان هذا **خلطًا بين
محورين مختلفين** وتم تصحيحه:

- إن DXVK/vkd3d-proton (التقنية وراء Proton الخاصة بـ Valve /
  توافقية ألعاب DirectX على Linux Steam) وCrossOver/Whisky
  (macOS) المعتمدة على MoltenVK، هي أمثلة حقيقية وعاملة تمامًا على
  نفس التوافقية بالاتجاه المعاكس التي يريدها المستخدم: تشغيل ملفات
  ثنائية/ألعاب DirectX (API الحصري لـ Windows) الحقيقية الموجودة
  دون تعديل على Linux/macOS.
- **اتجاه الترجمة** ("تُترجَم استدعاءات DirectX إلى استدعاءات
  Vulkan") و**اتجاه تجربة المستخدم النهائي** ("هل يعمل تطبيق DirectX
  المُستهدَف لـ Windows فعليًا على Linux/macOS") محوران منفصلان — كون
  الأول مُستهدِفًا لـ Vulkan لا يمنع الثاني من تحقيق "تشغيل DirectX
  على نظام تشغيل آخر" بشكل صحيح.
- لذلك، فإن استخدام Vulkan كخلفية تنفيذ داخلية لا يتعارض مع متابعة
  طبقة توافقية حقيقية بالاتجاه المعاكس — DXVK وغيرها هي بالضبط تلك
  السابقة. يتبنى هذا المشروع نفس النهج: اعتراض استدعاءات API الخاصة
  بـ D3D + ترجمة bytecode الخاص بتظليل DXBC/DXIL وقت التشغيل ← تنفيذ
  عبر Vulkan.

## النطاق وخارطة الطريق الصادقة

**المرحلة 0 (الحالية، مرحلة التصميم/البحث)**:
- بحث بنية DXBC/DXIL (تنسيقات bytecode الخاصة بتظليل DirectX).
- دراسة معمارية تنفيذات OSS الموجودة (DXVK، vkd3d-proton،
  [dxil-spirv](https://github.com/HansKristian-Work/dxil-spirv) —
  الأداة الفعلية لترجمة DXIL→SPIR-V التي يستخدمها vkd3d-proton،
  SPIRV-Cross، naga) لتجنب إعادة اختراع العجلة واقتباس قرارات تصميم
  مُثبتة.
- تحديد نطاق MVP واقعي: **خط أنابيب رسوميات كامل (rasterizer،
  عيّنة نسيج، حالة مزج، إلخ) خارج النطاق حاليًا.** البدء بشريحة
  رأسية (vertical slice) تغطي فقط إرسال D3D11 Compute Shader
  (DirectCompute) — تظليل حوسبة بسيط واحد يُترجَم فعليًا من DXBC/DXIL
  إلى SPIR-V، يُرسَل عبر `opencuda-vulkan` الخاصة بـ `open-cuda`،
  ويُتحقَّق من تطابقه عدديًا مع تنفيذ مرجعي على CPU. يبدأ العمل على
  خط أنابيب الرسوميات كمرحلة تالية فقط بعد إثبات هذه الشريحة الرأسية.

**من المرحلة 1 فصاعدًا (لم تبدأ بعد)**:
1. الشريحة الرأسية لـ D3D11 Compute Shader (ترجمة DXBC/DXIL→SPIR-V +
   إرسال Vulkan).
2. خط أنابيب رسوميات D3D11 الأدنى (تظليلات رأسية/بكسل + رسترة
   أساسية).
3. دعم D3D12 (قوائم الأوامر، كومات الواصفات، توقيعات الجذر).
4. دعم Android (Vulkan نفسه أصلي على Android، لذا يُتوقَّع إعادة
   استخدام معظم أصول Linux — لكن يُرجَّح الحاجة إلى طبقة محاكاة
   Win32/COM، ما يعادل Wine، وفي تلك الحالة يُنظَر في التعاون مع/
   إعادة استخدام مشروع Wine نفسه).
5. دعم macOS / iPhone / iPad (عبر MoltenVK، نفس نهج
   CrossOver/Whisky؛ يتطلب iPhone/iPad برنامج Apple Developer
   للتوزيع الرسمي — التشغيل الأصلي على أجهزة غير رسمية مستحيل، نفس
   القيد الذي حدّده بحث `dream-os`).
6. أنظمة عائلة UNIX المتنوعة (BSD، إلخ) — يُرجَّح إمكانية إعادة
   استخدام معظم مسار Linux حسب دعم Vulkan (لم يُبحَث بعد).

**بخصوص دعم PlayStation 4/5/6/7 (إفصاح صادق، اعتبارًا من
2026-07-25)**: مُدرَج في رؤية المستخدم الأصلية، لكن **توجد مخاوف
قانونية/شروط خدمة مستقلة عن الصعوبة التقنية** — SDKs الخاصة بتطوير
PlayStation خاصة وخاضعة لاتفاقيات عدم إفصاح (NDA)، والهندسة العكسية
غير الرسمية تحمل خطر انتهاك شروط خدمة وقوانين متعددة (مثل DMCA).
يُبقي هذا المشروع دعم PS4-7 مذكورًا في خارطة الطريق فقط كـ**"طموح
مستقبلي"** ولا يُدرِجه حاليًا في نطاق التصميم/التنفيذ. سيتطلب بدؤه
تقييم مخاطر قانونية منفصلًا وتأكيدًا متجددًا من المستخدم.

**بخصوص دعم Nintendo Switch 2/3 (أُضيف 2026-08-17، إفصاح صادق)**:
يُسجَّل بالمثل فقط كـ"طموح مستقبلي". يتطلب Switch 2 عتاد تطوير/NDA
رسميين من Nintendo (نفس المخاوف القانونية كـ PS4-7). **لم تُعلن
Nintendo رسميًا عن Switch 3 حتى 2026-08-17 — إدراجه هنا مجرد عنصر
نائب في حال/عند الإعلان عنه، وليس مبنيًا على معلومات منتج حقيقية**
(مذكور صراحةً لتجنب المبالغة).

## المشاريع الأساس (بتعليمات من المستخدم، 2026-07-25)

- **[open-cuda](https://github.com/aon-co-jp/open-cuda)**: يستخدم
  `opencuda-vulkan` (خلفية تنفيذ Vulkan Compute، مُتحقَّق منها على
  جهاز NVIDIA GT 730 حقيقي) كخلفية تنفيذ التظليل. يخطط لإعادة
  استخدام تجريد `opencuda-core::GpuDevice` (alloc/memcpy/
  launch_kernel) كما هو، مُمرِّرًا النوى (kernels) المُترجَمة من
  DXBC/DXIL→SPIR-V كـ `KernelSource::SpirV` (تفاصيل API الدقيقة
  تُؤكَّد لاحقًا مقابل `opencuda-core`). منفصل عن `opencuda-directx`
  (خلفية D3D12 حصرية لـ Windows، المرحلتان 1 و2 مُنفَّذتان بالفعل) —
  تلك تُشغِّل DirectX أصليًا *على* Windows، الاتجاه المعاكس لهذا
  المشروع (تشغيل DirectX *على أنظمة تشغيل أخرى*).
- **[aruaru-llm](https://github.com/aon-co-jp/aruaru-llm)**: لا
  اعتمادية تقنية مباشرة حاليًا (aruaru-llm خدمة استدلال LLM؛ هذا
  المشروع طبقة توافقية لـ API الرسوميات). النية الدقيقة وراء إدراج
  المستخدم لـ aruaru-llm كـ"أساس" غير مؤكَّدة — يحتمل أن تعني اتباع
  نمط خدمة "بونشين نو جوتسو" (clone/tenant) المشترك (مثل تطبيق نمط
  API إدارة أسلوب `TenantRegistry` الخاص بها على سطح إدارة ما في هذا
  المشروع، كخادم ذاكرة تخزين مؤقت للترجمة). سيُحدَّث هذا القسم بمجرد
  تحديد نقاط تكامل ملموسة.

## سياسة التطوير (ملخص على مستوى النظام البيئي)

- تنفيذ قائم على Rust بشكل أساسي. يستخدم حزمة `windows` (Windows
  API) وربطات Vulkan (`ash` إلخ، مطابقة لما تستخدمه
  `opencuda-vulkan`).
- "لا تُبلِّغ بالإنجاز اعتمادًا فقط على نجاح فحص الأنواع/البناء" —
  أبلِغ عن شيء ما بأنه يعمل فقط بعد ترجمة bytecode حقيقي من DXBC/DXIL
  فعليًا، وإرساله عبر Vulkan حقيقي، والتأكد من التطابق العددي مع
  تنفيذ مرجعي على CPU على جهاز حقيقي (انضباط على مستوى النظام
  البيئي بأكمله).
- يجب ألا تُشير الميزات غير المُنفَّذة/الوهمية زورًا إلى "مدعومة"
  (باتباع نمط `supports_dxil()` الخاص بـ `opencuda-directx`).
- التأكد من المستخدم قبل أي قرار يتعلق بملفات جديدة، أو حزم جديدة،
  أو مستودعات جديدة، أو أحكام التسمية/التموضع (درس من 2026-07-23،
  انظر CLAUDE.md الخاص بـ `open-raid-z`).

## HANDOFF (أحدث المدخلات فقط — انظر CLAUDE.md للسجل الكامل)

- **2026-08-20: تم تنفيذ والتحقق على عتاد حقيقي من شريحة عمودية
  لـ compute shader لعملية GEMM (بحجم ثابت 2x2)**، كخطوة أولى نحو
  التسريع بالعتاد لاستدلال نماذج اللغة الكبيرة (LLM). أُضيفت دالة
  `translate_gemm2x2_shader` إلى `directx-shader-translate`: تم فعليًا
  ترجمة شيدر HLSL لعملية GEMM 2x2x2 باستخدام `fxc.exe`، وفك ترميز
  DXBC الناتج، وترجمته إلى SPIR-V، ثم تشغيله فعليًا على Vulkan حقيقي
  (NVIDIA GT 730) باستخدام عقد النواة (kernel) الخاص بـ
  `opencuda-vulkan`. طابقت مخرجات وحدة معالجة الرسومات تمامًا نتيجة
  ضرب المصفوفات المرجعية على المعالج. قيد صادق: حجم ثابت 2x2 فقط —
  عملية GEMM العامة بحجم متغير (تتضمن حلقات)، وعمليات Attention،
  والربط مع `aruaru-llm` تبقى خارج نطاق هذه المرحلة.
- **2026-08-19: دراسة جدوى آلية تحديث تلقائي (على غرار التحديث الذاتي
  عبر GitHub Releases في `open-english`) — تم تأجيلها.** الملف
  التنفيذي الوحيد الذي يحتوي على `fn main` في مساحة العمل هو
  `directx-graphics-window` (عرض توضيحي بأسلوب Breakout للفحص
  البصري) بلا خدمة دائمة أو نقطة نهاية `/healthz`، لذا لا ينطبق النمط.
- **2026-08-19: دراسة الاعتراض القائل بأن "غياب خدمة خلفية دائمة يُعد
  عيبًا" — الخلاصة: لا.** يُوزَّع DirectX الحقيقي من مايكروسوفت على
  شكل ملفات DLL وقت التشغيل تربطها التطبيقات ديناميكيًا، وليس كخدمة
  خلفية مستقلة؛ التصميم الحالي (مكتبة + ملف تنفيذي توضيحي فقط) يطابق
  بنية DirectX الأصلي.
- **2026-08-20: تسجيل فكرة `open-cg-cad` كمذكرة (لم يبدأ العمل بعد).**
  تصور لأداة نمذجة ثلاثية الأبعاد مستقبلية مع التقاط الحركة، وتعديل
  المواصفات بلغة طبيعية عبر محادثة بالذكاء الاصطناعي، ودعم
  DirectX/OpenGL/WebGL/WebGPU.
- **2026-08-20 (متابعة): تسجيل تصور "عقارات بالذكاء الاصطناعي × مقاولات
  بالذكاء الاصطناعي"**، الذي يجمع بين `open-cg-cad` و`aruaru-llm`
  لتوليد نماذج ثلاثية الأبعاد تلقائيًا (منازل، مبانٍ، جسور، أنفاق، إلخ)
  من بيانات الأرض؛ ثم تم توسيعه لاحقًا ليشمل قطارات الرفع المغناطيسي
  ونماذج CAD لأشباه الموصلات (CPU/GPU/NPU)، مع ملاحظة صادقة حول صعوبة
  تغطية الهندسة المعمارية والمدنية والمركبات السكية وتصميم أشباه
  الموصلات في نظام واحد. مجرد مذكرة فكرة، لم يبدأ العمل بعد.

- **2026-08-08 (متابعة 12): تم تنفيذ تحميل نسيج من ملف PNG حقيقي،
  والتحقق منه على جهاز Windows + Linux حقيقيَين.** تُنفِّذ وحدة
  جديدة `png_loader.rs` (باستخدام حزمة `png`، إصدار 0.17.x)
  `load_png_rgba8`، وتُطبِّع صور RGB/RGBA/تدرج رمادي/تدرج رمادي+ألفا/
  لوحة ألوان إلى RGBA8 (توسيع لوحة الألوان وتطبيع 16بت→8بت مُفوَّضان
  إلى تحويلات حزمة `png` نفسها، وليسا مكتوبين يدويًا). تم توليد
  والتزام أصل اختبار حقيقي (`assets/sample_sprite.png`، رقعة شطرنج
  2x2 بربع واحد شبه شفاف). تم التحقق على جهاز Windows حقيقي
  (NVIDIA GT 730) وجهاز Linux حقيقي (WSL2 Ubuntu/Mesa llvmpipe):
  تطابقت الأرباع المعتمة تمامًا، وأنتج الربع شبه الشفاف لون التركيب
  المتوقع بالضبط عبر مزج ألفا القياسي بصيغة "over". مساحة العمل
  بأكملها: `cargo build`/`clippy` نظيفة (0 تحذيرات)؛ ينجح
  `cargo test --workspace --release` في جميع الـ33 اختبارًا على جهاز
  حقيقي + 56 اختبار وحدة، بدون انحدار. إفصاح صادق: لم تُختبَر فعليًا
  صور PNG المتشابكة (interlaced) وصور PNG بعمق 16 بت للقناة (يُعتمَد
  عليها فقط عبر المعالجة التلقائية لحزمة `png`)؛ لا تستدعي
  `directx-graphics-window` (عرض النافذة الحقيقي) هذا المُحمِّل بعد
  (لا تزال نسيج لون واحد 1×1).

- **2026-08-08 (متابعة 11): تم تنفيذ مزج ألفا (sprites شبه شفافة)،
  والتحقق منه على جهاز Windows + Linux حقيقيَين.** تم تفعيل مزج ألفا
  القياسي بصيغة "over" في بناء خط أنابيب
  `render_sprites_and_read_back` (`SRC_ALPHA`/`ONE_MINUS_SRC_ALPHA`/
  `ADD`). بما أن `src.a=1.0` يعادل عدديًا `blend_enable=false`، تم
  التأكد من أن جميع اختبارات sprites المعتمة الموجودة تُنتِج نفس
  النتائج (تغيير غير مُخِلّ وإضافي). تحقق اختبار جديد من نتيجة صيغة
  "over" القياسية على جهاز حقيقي، وتطابقت النتائج تمامًا على كلا
  نظامي التشغيل. إفصاح صادق: هذا المزج مُفعَّل فقط في مسار
  `render_sprites_and_read_back` (خارج الشاشة) — لم يُحدَّث
  `directx-graphics-window` (عرض النافذة الحقيقي) بعد ليتوافق؛ يُدعَم
  فقط مزج "over" (بدون أوضاع إضافية/ضربية)؛ لم يُختبَر التفاعل بين
  اختبار العمق والمزج (هذه المرحلة بدون مخزن عمق، sprites ثنائية
  الأبعاد فقط).

للسجل الكامل جلسة بجلسة (بما في ذلك مرحلة خط أنابيب رسوميات D3D11
السابقة، والشريحة الرأسية لـ DXIL، وحلقة اللعبة بالنافذة الحقيقية +
swapchain + إدخال لوحة المفاتيح، والعديد من زيادات طول سلسلة DXBC/DXIL
مع فحص الحدود)، انظر [CLAUDE.md](CLAUDE.md) (اليابانية، النسخة
الرسمية).
