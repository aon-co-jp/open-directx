# PORTING.md — ما هو قابل لإعادة الاستخدام، من قِبَل من، وكيف (نسخة مختصرة)

> **ملاحظة**: هذه ترجمة مختصرة. يبقى الدليل التقني الكامل بتفاصيل
> الكود والمزالق متاحًا فقط في ملف [PORTING.md](PORTING.md) الأصلي —
> راجعه قبل تبنّي نمط ما فعليًا.

ملخص لأنماط التنفيذ القابلة لإعادة الاستخدام من هذا المشروع، لمن
يريد نقلها إلى مشروع آخر:

1. **`crates/directx-shader-translate`**: مُحلِّل حاوية/قطعة DXBC
   (bytecode التظليل الخاص بـ D3D9/10/11، حتى SM5.1)
   (`parse_dxbc`)، بالإضافة إلى مُترجِم DXBC(SM5.0)→SPIR-V ضيق النطاق
   (`translate_shader`) خاص بالضبط بأشكال أكواد العمليات التي تصدرها
   فعليًا `fxc.exe` لتظليلات هذا المشروع نفسه: عمليات مفردة (جمع/
   ضرب/طرح-عبر-جمع-منفي/قسمة)، سلاسل عمليات ثنائية متسلسلة بطول
   عشوائي (معتمدة على شجرة تعبير سجل، وليس تثبيتًا صلبًا لكل طول —
   تم التحقق حتى 7 حدود بدون *أي* تغييرات في كود الإنتاج مطلوبة لكل
   عدد حدود جديد)، ومتغيرات مع فحص حدود (`if (id.x < N)`) من نفس ما
   سبق، تُولِّد SPIR-V حقيقي `OpSelectionMerge`/`OpBranchConditional`.
   يُرفَض أي شكل كود عملية آخر بصدق عبر
   `SpirvGenError::UnsupportedShader` بدلًا من ترجمته خطأً بصمت — هذا
   **ليس** فك ترميز عام لـ SM5.0.
2. **دعم DXIL (SM6+، D3D12)** (`src/dxil.rs`): تسير
   `parse_dxil_container` على بث بت LLVM الخام (عبر حزمة `llvm-
   bitcode` العامة) لحل جدول الأنواع وفك ترميز تعليمات الدالة؛ تفك
   عائلة دوال `resolve_vector_add_dxil_calls` غموض استدعاءات حدوس
   `dx.op.*` الخاصة بـ DXIL (CreateHandle/ThreadId/BufferLoad/
   BufferStore) مقابل أرقام أكواد عمليات DXIL.rst الموثّقة من
   Microsoft. تشترك `translate_dxil_..._to_spirv` في مُصدِر SPIR-V
   واحد (`emit_spirv_for_kernel`) مع خلفية DXBC، لذا تُنتِج كلتا
   حاويتَي التنسيق SPIR-V عبر مسار كود واحد. يُستخرَج حجم مجموعة
   العمل (`numthreads`) فعليًا من `METADATA_BLOCK` الخاص بـ DXIL
   (`dx.entryPoints` → `ShaderProperties`)، وليس مُثبَّتًا بشكل صلب.
   تم التحقق من تعميم السلسلة مع فحص الحدود (انظر أعلاه) من النهاية
   إلى النهاية على كل من DXBC وDXIL لسلاسل تصل حتى 7 حدود.
3. **خط أنابيب رسوميات D3D11**: تُصدِر `translate_vertex_shader`/
   `translate_pixel_shader` كود SPIR-V رسوميات حقيقيًا لزوج VS/PS
   تمريري ثابت، وتم التحقق منه بطريقتين مستقلتين (إعادة تحليل
   rspirv + `spirv-val.exe` الحقيقية من Vulkan SDK، وكلاهما برمز
   خروج 0). تضيف الحزمة الجديدة `crates/directx-graphics-vulkan`
   `ash` كاعتمادية **مباشرة** لهذا المشروع نفسه (وليست مُرصوفة فوق
   `opencuda-vulkan`، والتي تم التأكد عبر تدقيق المصدر أنها للإرسال
   الحاسوبي فقط) وتُنفِّذ ممر رسم حقيقيًا، وإطار عمل، و
   `VkGraphicsPipelineCreateInfo`. ترسم وتقرأ مثلثات ومشاهد نسيج/
   sprites متعددة مع مزج ألفا القياسي بصيغة "over" وتحميل نسيج من
   ملف PNG حقيقي (حزمة `png`)، وجميعها مُتحقَّق منها على جهاز NVIDIA
   GT 730 حقيقي (Windows) *و* WSL2 Ubuntu/Mesa llvmpipe (Linux)، مع
   تطابق النتائج على كلا نظامي التشغيل.
4. **`crates/directx-graphics-window`**: نافذة حقيقية + swapchain
   فيتلكان حقيقي + إدخال لوحة مفاتيح حقيقي (winit + ash-window)،
   تحمل مثيل/جهاز Vulkan مستقلًا خاصًا بها منفصلًا عن سياق
   `directx-graphics-vulkan` خارج الشاشة — حافظ على تزامن الاثنين
   عند استخدام كليهما (مثلًا مزج ألفا مُفعَّل حاليًا فقط في المسار
   خارج الشاشة). أنتجت نموذجًا أوليًا تفاعليًا على غرار Breakout، تم
   تأكيد عمله بعينَي المستخدم نفسه (حركة المضرب + ارتداد الكرة).
5. **اتفاقية اعتمادية المسار (path-dependency)**: تعتمد هذه الحزمة
   على `opencuda-core`/`opencuda-vulkan` الخاصتين بـ `open-cuda` عبر
   اعتماديات مسار نسبي (relative path)، على مستوى دليل واحد أعمق من
   اتفاقية "المستودعات الشقيقة تحت `F:\runo`" المستخدمة في مكان آخر
   من هذا النظام البيئي (مثل `aruaru-llm`، `aruaru-db`) — هذه
   **اعتماديات تطوير فقط (dev-dependencies)**، والمكتبة المنشورة
   نفسها لا تملك أي اعتمادية على `open-cuda`.
6. **ملاحظة نطاق مكافحة الغش على مستوى النواة**: لمن يريد نقل هذا
   المشروع نحو هدف "تشغيل ألعاب Windows الحقيقية على Linux" — تمنع
   أنظمة مكافحة الغش على مستوى النواة (Riot Vanguard، BattlEye بوضع
   النواة، إلخ) بيئات على غرار Linux/Proton بحكم التصميم، بغض النظر
   عن مدى اكتمال طبقة ترجمة التظليل هذه. هذا ليس عيبًا يجب إصلاحه؛
   الألعاب التي تستخدم مكافحة الغش من هذا النوع خارجة عن متناول هذا
   المشروع بغض النظر عن اكتمال الترجمة.

## ما هو غير قابل لإعادة الاستخدام بعد (فجوات صادقة)

- لا يوجد فك ترميز عام لتعليمات SM5.0 أو SM6.0 — تُعالَج فقط فئات
  أشكال/أكواد العمليات المحدَّدة الموصوفة أعلاه؛ يُرفَض أي شيء آخر
  بصدق، وليس يُترجَم خطأً.
- طبقات D3D12 الأعلى مستوى (قوائم الأوامر، كومات الواصفات، توقيعات
  الجذر) غير مُنفَّذة بالكامل.
- لا يوجد مخزن عمق، ولا فحص استيفاء عبر رؤوس ملوّنة بشكل مختلف يتجاوز
  حالة المثلث المتدرج، ولا يزال عتاد GPU الخاص بـ AMD وIntel والتنفيذ
  الأصلي على macOS/Linux لسطح المكتب غير مُتحقَّق منه (توجد مسارات
  كود لكشف PCI vendor-ID الخاص بـ AMD/Intel لكنها لم تُشغَّل أبدًا
  مقابل عتاد AMD/Intel حقيقي في بيئة التطوير هذه).

---

للتفاصيل التقنية الكاملة (تسلسلات أكواد العمليات الدقيقة، تتبعات بث
بت DXIL على مستوى البايت، مقتطفات الكود، وأمثلة Cargo.toml الكاملة
لاعتمادية المسار)، انظر ملف [PORTING.md](PORTING.md) الأصلي
(الإنجليزية، النسخة الرسمية).
